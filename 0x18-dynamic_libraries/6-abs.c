#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 *  @n: the init in question
 *
 * Return: the abs of int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
