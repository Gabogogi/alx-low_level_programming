#include <stdio.h>
#include <main.h>
/**
 * main - Entry point
 * Description: 'the program's description'
 * Return: Always 0 (success)
 */
int main(){
	char c;
	for(c = 'a'; c <= 'z'; c++){
		putchar(c);
	}
	putchar('\n');
return (0);
}
